import React from 'react'
import { useSelector } from 'react-redux';

const SearchPanel = () => {
    const { SeachPanel } = useSelector(state => state.Features);

  return (
    <div>
      
    </div>
  )
}

export default SearchPanel
